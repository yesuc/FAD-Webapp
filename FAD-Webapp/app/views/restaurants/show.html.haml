
%body
  %p#notice= notice
  %div.row
    .col-md-offset-1
    .col-md-4
      %h3.header.name= "#{@restaurant.name}"
  %div.row
    .col-md-offset-2
    .col-md-8.url= "#{@restaurant.url}"
  %div.row
    .col-md-offset-2
    .col-md-8.address= "#{@restaurant.address}"
  %div.row
    .col-md-offset-2
    .col-md-8= "#{@restaurant.cuisine}"
  %div.row
    .col-md-offset-2
    .col-sm-8= "#{@restaurant.latitude} N, #{@restaurant.longitude} W"
  %div.row
    =session[:tags]
    -@foods.each do |f|
      %h4="#{f.name}"
      %br
      ="#{f.description}"
      %br
      ="#{f.ingredients.split().join(', ')}"
      %br








-# = link_to 'Edit', edit_restaurant_path(@restaurant)
\|
= link_to 'Back', restaurants_path
